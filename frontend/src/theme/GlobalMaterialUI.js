import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#7ec0e4", contrastText: "#FFFF" },
    secondary: { main: "#f02a71"}
  }
});

export default theme;
